# FireSight UI

This is a code bundle for FireSight UI. The original project is available at https://www.figma.com/design/bzHbRw1kmg4FiwGkAP2j4z/FireSight-UI.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
